version: "3.9"

services:
  database:
    image: mysql:8.0
    container_name: mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: project
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin
    command: >
      mysqld --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"   # host:container
    volumes:
      - /Users/mmintah/Desktop/virtualenv/opt/project/mysql/data:/var/lib/mysql
    networks:
      - project
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 40s

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: phpmyadmin
    restart: unless-stopped
    depends_on:
      database:
        condition: service_healthy
    environment:
      PMA_HOST: database
      PMA_PORT: 3306
      PMA_ARBITRARY: "1"
      UPLOAD_LIMIT: 256M
      SESSION_SAVE_PATH: /sessions
    ports:
      - "8080:80"     # http://localhost:8080
    volumes:
      - /Users/mmintah/Desktop/virtualenv/opt/project/phpmyadmin/sessions:/sessions
      - /Users/mmintah/Desktop/virtualenv/opt/project/phpmyadmin/uploads:/var/www/html/upload
      - /Users/mmintah/Desktop/virtualenv/opt/project/phpmyadmin/saves:/var/www/html/save
    networks:
      - project

networks:
  project:
    driver: bridge